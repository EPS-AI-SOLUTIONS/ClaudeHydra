# HYDRA 10.6.1 - Development Commands

## Slash Commands

### Dashboard & Witcher
| Command | Description |
|---------|-------------|
| `/dashboard` | Show Multi-CLI Control Center |
| `/witcher <task>` | Unite ALL CLIs for complex tasks |

### External CLI Providers
| Command | Provider | Usage |
|---------|----------|-------|
| `/gemini <query>` | Google | 2M context, multimodal |
| `/jules <task>` | Google | Async background tasks |
| `/codex <task>` | OpenAI | GPT-5 code generation |
| `/grok <query>` | xAI | Real-time analysis |
| `/deepseek <task>` | DeepSeek | 100+ languages |

### HYDRA Core
| Command | Description |
|---------|-------------|
| `/hydra [task]` | Full orchestration |
| `/ai <query>` | Quick local AI ($0) |
| `/ai-status` | Check all providers |
| `/ai-models` | Load available models |
| `/swarm <query>` | Multi-agent protocol |

### Advanced
| Command | Description |
|---------|-------------|
| `/self-correct` | Code with auto-validation |
| `/speculate` | Model racing |
| `/semantic-query` | Deep RAG |
| `/few-shot` | Learn from history |
| `/yolo` | Toggle YOLO mode |
| `/memory` | Manage Serena memories |

## Build Commands

### Tauri Launcher
```bash
# Development
cd hydra-launcher && npm run tauri dev

# Build
cd hydra-launcher && npm run tauri build

# Install dependencies
cd hydra-launcher && npm install
```

### Scripts
```bash
# Initialize HYDRA
node .claude/scripts/hydra-init.js

# Load models
node .claude/scripts/model-loader.js status

# Update memories
node .claude/scripts/memory-manager.js update
```

## Git Workflow

```bash
# Feature branch
git checkout -b feat/feature-name

# Commit
git add . && git commit -m "feat(scope): description"

# Push
git push -u origin feat/feature-name
```

## Testing

```bash
# Tauri
cd hydra-launcher && npm test

# Lint
cd hydra-launcher && npm run lint
```

---
*Auto-generated by HYDRA Memory System*
