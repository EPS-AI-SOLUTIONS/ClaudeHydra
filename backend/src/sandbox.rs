// Jaskier Shared Pattern — sandbox (thin re-export)
//
// All sandbox logic lives in the `jaskier-sandbox` shared crate.
// This module re-exports everything for backward compatibility.

pub use jaskier_sandbox::*;
